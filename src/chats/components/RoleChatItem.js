import React from 'react';
import { View } from 'react-native';
import { useTheme } from '@react-navigation/native';

const RoleChatItem = () => {
  const { colors } = useTheme();

  const styles = {
    container: {

    },    
  };

  return (
    <View>
      
    </View>
  );
}

export default RoleChatItem;