{
 "secret":"Create your own secret", //for validate request in both side
 "admin_token":"Create own token", // for verify admin
 "attr_sign":"",
 "auth_user":" ", // api if is middleware
 "supervisor_api":" ", // api if is middleware
 "allow_size":"", // file size allowed
 "event_delete":" ", // event delete message secret
 "event_close":" ", // event close chat secret
 "notification":" ", // notification token for api request 
 "path":{
    "1":"",
    ...
 } // path if is middlewarel
}
